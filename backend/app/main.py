"""
FastAPI application entry point.
Full implementation delivered in Phase 3.
This stub exists so Alembic can import app.models without a missing-module error.
"""
from fastapi import FastAPI

app = FastAPI(title="LX-OS API")
